<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="milton.atividadeormlite">

    <application
        android:allowBackup="true"
        android:banner="@drawable/ic_laucher_novo"
        android:icon="@drawable/ic_laucher_novo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.CadastroEletrodomesticoActivity" />
        <activity android:name=".View.CadastrarUtilizacaoActivity"></activity>
    </application>

</manifest>
